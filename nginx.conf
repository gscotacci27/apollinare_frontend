server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/html text/css application/javascript application/json image/svg+xml;
    gzip_min_length 256;

    # Hashed assets in /assets/ — cache forever, mai fallback su index.html
    location /assets/ {
        try_files $uri =404;
        expires 1y;
        add_header Cache-Control "max-age=31536000, immutable";
    }

    # Service worker — never cache (browser must always re-fetch)
    location = /sw.js {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Service-Worker-Allowed "/";
    }

    # manifest.json — short cache
    location = /manifest.json {
        add_header Cache-Control "no-cache";
    }

    # SPA fallback — forza pulizia cache browser ad ogni load
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-store, no-cache, must-revalidate";
        add_header Pragma "no-cache";
        add_header Expires "0";
        add_header Clear-Site-Data '"cache"';
    }
}
